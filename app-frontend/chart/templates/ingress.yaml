apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: app-frontend-ingress
  namespace: {{ .Release.Namespace }}
  annotations:
    kubernetes.io/ingress.class: "nginx"
    # 🔧 Indique à Nginx de surcharger le Host envoyé au conteneur Flask
    nginx.ingress.kubernetes.io/upstream-vhost: "127.0.0.1"
spec:
  ingressClassName: nginx
  rules:
  - host: {{ .Values.ingress.host }}
    http:
      paths:
      - path: /
        pathType: Prefix
        backend:
          service:
            name: app-frontend-preprod-service
            port:
              number: 5000