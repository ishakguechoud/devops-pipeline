containerPort: 5001

image:
  registry: "192.168.74.128:8082"
  repository: "app-users"
  tag: "latest"
  pullPolicy: IfNotPresent

service:
  type: NodePort
  port: 5001
  nodePort: 30001

labels:
  environment: dev

serviceAccount:
  name: "default"

# Variables d'environnement injectées dans le container
# En preprod : credentials passés directement ici
# En prod    : credentials injectés depuis Vault (podAnnotations + script de démarrage)
env: {}

podAnnotations: {}